---
task_id: T-023
epic: E-5
title: Thinking 步骤展示
depends_on: [T-016, T-017]
estimated_minutes: 60
files_touched:
  - src/components/Prompt/PromptInput.tsx
files_read_only:
  - src/services/nanoBananaApi.ts
  - src/contexts/ConfigContext.tsx
forbidden_paths:
  - src/components/common/**
  - src/pages/MainPage.tsx
acceptance:
  - 使用 nano-banana-pro 或 thinking_level > minimal 时，展示 thinking 步骤
  - 展示为可折叠列表，格式：[Step N] text...
  - 加载完成后可收起，不阻塞主流程
---

## Context
Gemini 返回的 thinking 步骤需要展示给用户。

## Prerequisites
- T-016 generateWithThinking API 完成
- T-017 thinking_level 参数控件完成

## Steps
1. 修改 PromptInput：
   - 添加 thinkingSteps 状态
   - 调用 generateWithThinking 替代 generateImage（当 thinking 可用时）
   - 显示 ThinkingStepsDisplay 组件
2. 新建 ThinkingStepsDisplay 组件：
   - 列表形式展示每个步骤
   - 可折叠/展开
   - 格式：`[Step N] ...text...`
3. 完成后可收起，不影响图片显示

## Acceptance
- thinking 步骤正确展示
- 可折叠/展开
- 不阻塞图片显示

## Out of Scope
- 与版本管理、串联功能冲突的部分

## Examples
```
[Step 1] Analyzing the input image...
[Step 2] Decomposing the prompt into visual elements...
[Step 3] Selecting appropriate style parameters...
```
